# This directory holds figures regenerated by the analysis scripts.
# Its contents are git-ignored (see .gitignore); this file keeps the empty
# directory in version control so scripts can write here on a fresh clone.
